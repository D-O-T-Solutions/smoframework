# acl/

Access control and policy enforcement. Capability-based, not role-based.

## Modules
- `policy/`     — Local policy engine (capability resolution) (§X)
- `presets/`    — Predefined capability groupings (Reader, Contributor, Authority)
- `revocation/` — Capability revocation propagation
